import status from './status'
import tags from './tags'
import autoTag from './auto-tag'
const leads = {
    status: Object.assign(status, status),
tags: Object.assign(tags, tags),
autoTag: Object.assign(autoTag, autoTag),
}

export default leads